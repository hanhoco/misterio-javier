{
  "name": "detective-lab-skeleton",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Walking skeleton: hidden colour seals on a poster, decoded from a pasted screenshot.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "node scripts/run-tests.mjs",
    "build:pages": "PUBLIC_BASE=/misterio-javier/ npm run build",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
